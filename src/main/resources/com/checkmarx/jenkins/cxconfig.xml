<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
    <entry key="MaxZipSizeBytes">209715200</entry>
    <entry key="MaxOSAZipSizeBytes">2146483647</entry>
    <entry key="DefaultFilterPattern">!**/_cvs/**/*, !**/.svn/**/*,   !**/.hg/**/*,   !**/.git/**/*,  !**/.bzr/**/*, !**/bin/**/*,
!**/obj/**/*,  !**/backup/**/*, !**/.idea/**/*, !**/*.DS_Store, !**/*.ipr,     !**/*.iws,
!**/*.bak,     !**/*.tmp,       !**/*.aac,      !**/*.aif,      !**/*.iff,     !**/*.m3u, !**/*.mid, !**/*.mp3,
!**/*.mpa,     !**/*.ra,        !**/*.wav,      !**/*.wma,      !**/*.3g2,     !**/*.3gp, !**/*.asf, !**/*.asx,
!**/*.avi,     !**/*.flv,       !**/*.mov,      !**/*.mp4,      !**/*.mpg,     !**/*.rm,  !**/*.swf, !**/*.vob,
!**/*.wmv,     !**/*.bmp,       !**/*.gif,      !**/*.jpg,      !**/*.png,     !**/*.psd, !**/*.tif, !**/*.swf,
!**/*.jar,     !**/*.zip,       !**/*.rar,      !**/*.exe,      !**/*.dll,     !**/*.pdb, !**/*.7z,  !**/*.gz,
!**/*.tar.gz,  !**/*.tar,       !**/*.gz,       !**/*.ahtm,     !**/*.ahtml,   !**/*.fhtml, !**/*.hdm,
!**/*.hdml,    !**/*.hsql,      !**/*.ht,       !**/*.hta,      !**/*.htc,     !**/*.htd, !**/*.war, !**/*.ear,
!**/*.htmls,   !**/*.ihtml,     !**/*.mht,      !**/*.mhtm,     !**/*.mhtml,   !**/*.ssi, !**/*.stm,
!**/*.stml,    !**/*.ttml,      !**/*.txn,      !**/*.xhtm,     !**/*.xhtml,   !**/*.class, !**/*.iml, !Checkmarx/Reports/*.*</entry>
    <entry key="ServerCallRetryNumber">5</entry>
    <!-- requestTimeOutDuration is in seconds -->
    <entry key="RequestTimeOutDurationSec">60</entry>
    <entry key="OSASupportedExtensions">jar, war, ear, aar, dll, exe, msi, nupkg, egg, whl, tar.gz, gem, deb, udeb,
dmg, drpm, rpm, pkg.tar.xz, swf, swc, air, apk, zip, gzip, tar.bz2, tgz, c, cc, cp, cpp, css, c++, h, hh, hpp,
hxx, h++, m, mm, pch, c#, cs, csharp, go, goc, js, plx, pm, ph, cgi, fcgi, psgi, al, perl, t, p6m, p6l, nqp,6pl, 6pm,
p6, php, py, rb, swift, clj, cljx, cljs, cljc</entry>
    <entry key="OSAExtractableExtensions">zip, war, ear, tgz</entry>
</properties>